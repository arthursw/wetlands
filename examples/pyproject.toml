[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "wetlands-example-workers"
version = "0.0.0"
requires-python = ">=3.9"

[tool.setuptools]
py-modules = ["example_module", "minimal_module"]
